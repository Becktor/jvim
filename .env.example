# User/Group IDs - auto-detected by run.sh/build.sh
# Only set manually if needed:
# USER_UID=1000
# USER_GID=1000
# USERNAME=nvim

# Dotfiles repository
DOTFILES_REPO=https://github.com/Becktor/dotfiles.git
DOTFILES_BRANCH=main

# Workspace directory to mount
WORKSPACE=~/projects

# SSH directory (for git operations)
SSH_DIR=~/.ssh

# Git config file
GITCONFIG=~/.gitconfig
